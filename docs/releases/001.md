# 版本：001

状态：开发中

类型：混合

## 初始状态

- 起点分支：`develop`
- 初始 commit：`ec4560bfdd0adfdd3c2f9d5cb8633b6d533dc869`
- 开发分支：`dev/001`
- 上一成功版本：无，这是首个正式版本。

## 本版本目标

- 建立 MailOps 单仓库版本、候选镜像和生产发布流程。
- 将当前已实现的 MailOps 功能作为首个生产候选版本。
- 生产采用 backend/frontend 双镜像 immutable digest 和 SQLite 备份后的短暂停机升级。
- 停止跟踪本机 SQLite 运行文件，版本内容只保留 schema 和 migrations。

## 候选内容

- source commit：待开发完成后填写。
- compose commit：待候选 digest 写入后填写。
- backend image digest：待 `GHCR Images` workflow 完成后填写。
- frontend image digest：待 `GHCR Images` workflow 完成后填写。

## 验证记录

- 自动化验证：2026-08-15 在 `dev/001` 工作区运行 `npm run validate` 通过；backend typecheck、frontend lint、后端 19 项测试、前端 11 项测试及双端 production build 均通过。Vite 仍报告约 1.26 MB 主 bundle 的既有 warning。
- 发布配置静态验证：全部 GitHub Actions YAML 使用项目已安装的 `yaml` parser 解析通过。
- 生产 Compose 验证：`docker compose --env-file deploy/images.env -f deploy/docker-compose.yml config --quiet` 通过。
- 本地 exact-digest Docker 验证：待执行。
- 服务器候选检查：待执行。
- SQLite 备份：待执行。

## 生产结果

- 部署结果：待执行。
- 回滚目标：部署前确认当前服务器状态和上一组可用 digest；首版上线前不得假设存在可用 MailOps 回滚版本。
- 备注：生产部署、commit、push、tag 和镜像提升均需单独确认。
