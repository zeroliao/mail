# 版本：001

状态：成功

类型：混合

## 初始状态

- 起点分支：`develop`
- 初始 commit：`ec4560bfdd0adfdd3c2f9d5cb8633b6d533dc869`
- 开发分支：`dev/001`
- 上一成功版本：无，这是首个正式版本。

## 本版本目标

- 建立 MailOps 单仓库版本、候选镜像和生产发布流程。
- 将当前已实现的 MailOps 功能作为首个生产候选版本。
- 生产采用 backend/frontend 双镜像 immutable digest 和 SQLite 备份后的短暂停机升级。
- 停止跟踪本机 SQLite 运行文件，版本内容只保留 schema 和 migrations。

## 候选内容

- source commit：`b0796be0723678417d63b4e3eca09a0612b97992`。
- compose commit：`cb5ef00fcd7ec3017e6118f6afa1c959ae9e9b01`。
- backend image digest：`ghcr.io/zeroliao/mail-backend@sha256:b2c8920f8119f0cf7601486970d016d9053182429bc2a88c1e573b4ebbbf240c`。
- frontend image digest：`ghcr.io/zeroliao/mail-frontend@sha256:689d1586efa82989de22fc3774f0e5078efd267865a3410bca3abd83bf47f3f6`。

## 验证记录

- 首轮 release CI：GitHub Actions run `31888741653` 未通过。原因是测试依赖已停止跟踪的 SQLite fixture，且 CI 未显式提供测试环境变量；这两个问题随后均已在 `dev/001` 修复。
- 修复验证：2026-08-15 在 `dev/001` 工作区运行 bind OAuth targeted test 通过（8/8）；backend typecheck、frontend lint、后端 19 项测试、前端 11 项测试及双端 production build 均通过。Vite 仍报告约 1.26 MB 主 bundle 的既有 warning。
- release 门禁：CI run `31889545360` 和 GHCR Images run `31889545291` 均通过；两个候选镜像均由 source commit `82d0964d0192f61b114fc556d07d324a9370889a` 构建。
- 首轮服务器隔离验证：旧候选 health、digest 和资源检查通过，但 backend 日志出现 Prisma 无法检测 OpenSSL 的 runtime warning，因此未部署生产并清理了隔离实例。
- 修复后门禁：CI run `31891729147` 和 GHCR Images run `31891729164` 均通过；两个新候选镜像均由 source commit `8733507d4b888d994a825613c63a565b120b4ddb` 构建。
- 第二轮服务器隔离验证：安装 OpenSSL 3 后 backend 暴露 Prisma Client 只包含 OpenSSL 1.1 engine 的构建/runtime 不一致，隔离实例未通过 health 并已清理，生产仍未部署。
- 最终候选门禁：CI run `31892369482` 和 GHCR Images run `31892369444` 均通过；候选同时包含 runtime OpenSSL 3 和 `debian-openssl-3.0.x` Prisma engine。
- 发布配置静态验证：全部 GitHub Actions YAML 使用项目已安装的 `yaml` parser 解析通过。
- 生产 Compose 验证：`docker compose --env-file deploy/images.env -f deploy/docker-compose.yml config --quiet` 通过。
- 本地 exact-digest Docker 验证：最终候选未在本地单独重复执行；改由目标服务器隔离实例直接验证同一组 immutable digest。
- 服务器最终候选隔离验证：backend/frontend 使用上述 immutable digest 启动成功，health、实际 digest 和日志检查通过；无 OpenSSL warning、Prisma initialization error、`ERROR` 或 `FATAL`，验证后的隔离实例已清理。
- 服务器资源检查：部署前后均保留超过 1 GiB available memory；部署后约 `1526 MiB` available。实际容器内存约为 backend `129.6 MiB`、frontend `4.2 MiB`，未触及 Compose 的 `768 MiB`/`128 MiB` 限制。
- SQLite 初始导入备份：`/opt/mailops-deploy/backups/001-initial-import-20260815T224320/dev.db`，SHA-256 为 `c28752f29d61724d4303ab4540f34ebba893489140cf641fbd6b8b04827017b0`，`integrity_check=ok`。
- 数据迁移核对：本地和服务器备份 checksum 一致；共迁移账号 135 行，其中可见账号 134、soft-deleted 账号 1，缓存邮件 78 行。公网账号列表显示 134 条，符合 soft-delete 过滤规则。

## 生产结果

- 部署结果：2026-08-15 已成功部署到 `https://mail.zero007.chat`，生产目录为 `/opt/mailops-deploy`，运行 `mailops-backend-1` 和 `mailops-frontend-1`，数据库使用 `mailops_mail_data` volume。公网首页、`/api/v1/health`、数据库 health、管理员登录和账号列表均验证通过；HTTP 到 HTTPS 返回 `308`，origin TLS 直连正常。
- 实际镜像：backend/frontend 均为本记录“候选内容”中的 immutable digest，生产 Compose 继续固定 digest，不依赖 mutable tag。
- 反向代理：Caddy 已增加 `mail.zero007.chat` 站点，将 `/api/*` 转发到 `127.0.0.1:13000`，其余请求转发到 `127.0.0.1:15173`；修改前配置备份为 `/etc/caddy/Caddyfile.pre-mailops-20260815T234000`，error-level 日志检查无异常。
- 回滚目标：这是首次 MailOps 部署，没有上一组可用 MailOps 镜像。需要撤回时停止 MailOps Compose，并恢复上述 Caddy 配置备份；只有在明确批准数据库恢复后，才使用初始导入备份覆盖生产 SQLite。
- 已知配置限制：Gmail OAuth 已配置；Microsoft global OAuth 未配置。迁移的 per-account 数据仍保留，但需要全局 Microsoft OAuth 的新增授权流程暂不可用。
- 备注：本地 MailOps 已通过 `stop.ps1` 停止，原本地数据库保留；生产 `.env` 权限为 `0600`，沿用既有 `TOKEN_ENCRYPTION_KEY`，未将 secrets 纳入 Git 或版本记录。
