# 版本：002

状态：开发中

类型：混合

## 初始状态

- 起点分支：`main`
- 初始 commit：`4d041672678bf2fc3edb6ae0e146481b39f5f147`
- 开发分支：`dev/002`
- 上一成功版本：`001`，归档 tag 为 `v001`。
- 回滚 backend digest：`ghcr.io/zeroliao/mail-backend@sha256:b2c8920f8119f0cf7601486970d016d9053182429bc2a88c1e573b4ebbbf240c`
- 回滚 frontend digest：`ghcr.io/zeroliao/mail-frontend@sha256:689d1586efa82989de22fc3774f0e5078efd267865a3410bca3abd83bf47f3f6`

## 本版本目标

- 在 CI 中真实启动 backend production image，覆盖 Prisma migration、health 和 OpenSSL runtime 兼容性。
- 统一本地与 CI runtime gate，并仅在 runtime 输入变化时运行 Docker 门禁。
- 为 Windows 本机提供可复用的 Docker Desktop 限时启动和精确失败清理入口，并让 `start.ps1 -Mode docker` 共用该实现。
- 统一本地与目标服务器候选验证规则，避免重复验证和门禁含义不一致。
- 补全首次 SQLite 数据导入、加密密钥连续性、临时文件清理和反向代理验证流程。
- 将 GitHub default branch 从历史 `develop` 调整为当前稳定分支 `main`。
- 明确候选构建、最终记录 CI、Release Archive 和可选镜像 tag 提升的边界。

## 候选内容

- source commit：待开发完成后记录。
- compose commit：待候选 digest 固定后记录。
- backend image digest：待 `GHCR Images` workflow 生成。
- frontend image digest：待 `GHCR Images` workflow 生成。

## 验证记录

- 本地检查：2026-08-16 backend typecheck、frontend lint、backend 19/19 tests、frontend 11/11 tests 和双端 production build 通过；Vite 保留约 1.26 MB 主 bundle 的既有 warning。
- 发布配置静态检查：两个 Bash runtime 脚本通过 Git Bash `bash -n`，`ensure-docker-desktop.ps1`、`validate-runtime-gate.ps1` 和 `start.ps1` 通过 Windows PowerShell 5.1 parser；CI YAML、package JSON、runtime 路径过滤和 local/production Compose `config --quiet` 均通过。
- GitHub 仓库设置：default branch 已改为 `main`；`CI`、`GHCR Images`、`Promote Verified Images` 和 `Release Archive` 均已注册为 active workflow。
- runtime smoke 调试总结：前两次失败分别暴露 clean checkout 对根 `.env` 的隐式依赖、build stage 缺少 OpenSSL，以及未创建容器时无法通过 `docker compose images -q` 取得 image。最终通过 optional env file、统一 OpenSSL base 和显式 `mailops-backend:local` tag 解决；CI run `31896969466` 成功完成 component checks、双端 build、Docker build 和 backend production image smoke。
- 流程改进：新增 `deploy/scripts/validate-runtime-gate.sh` 作为本地与 CI 的统一入口；runtime 输入必须在首次 push 前于 Docker-capable 环境验证，纯文档提交跳过 Docker gate，workflow 失败只跟踪当前 HEAD 的单一 run 并读取对应失败 step。
- 统一 runtime gate 验证：CI run `31898277277` 成功；runtime 输入检测和 `Validate Docker runtime gate` 均成功，skipped summary step 未执行。
- Windows Docker Desktop 经验固化：新增 `ensure-docker-desktop.ps1` 和 `validate-runtime-gate.ps1`；daemon 探测有单次超时，启动时记录基线进程，失败只清理本轮新增且 start time 匹配的进程，Git Bash 使用明确安装路径。
- Windows 本机 gate：`npm.cmd run validate:runtime` 成功；脚本按需启动 Docker Desktop，构建 `mailops-backend:local`/`mailops-frontend:local`，backend smoke 完成两个 migration、数据库 health 和 HTTP health，最近日志无 Prisma/OpenSSL runtime error。
- 候选隔离验证：待生成候选后执行。

## 生产结果

- 部署结果：待执行，当前版本尚未获得生产部署授权。
- SQLite 备份：待部署前记录。
- 回滚目标：`v001` 及上述两个 immutable digest；普通镜像回滚不得自动覆盖生产数据库。
- 已知风险与未验证项：本机 Docker daemon 未运行，backend production image 未在本地启动；GitHub CI 已在 Linux runner 完成同一 production image runtime smoke。当前版本不改变应用业务行为。
- 备注：本版本只在 `dev/002` 开发，未进入 `release/002`、`main` 或生产。
