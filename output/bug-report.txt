@产品经理 @质量与风险总 线上Bug（hotmail 取邮件 502）已按你的根因清单修复完成 ✅。两点根因均闭环：

【根因1 — 脏数据 + 测试隔离】
- 已软删除 mock 账号 e2e-owner@hotmail.com（id cmqf90oov...，置 ARCHIVED + deletedAt）。账号池现仅剩 2 个真实 Gmail（larapaul552 / wimslenita）均 ACTIVE。
- 因 getAuthorizedAccount→ensureRawAccount 过滤 deletedAt:null，该账号现在取邮件返回 404 而非 502，不再污染池。
- e2e 测试已改为自隔离：每次运行复制模板库到 prisma/test-bind-oauth-<ts>-<pid>.db、绑定前设置 DATABASE_URL、跑完删除——无论怎么调都不会再写真实 dev.db。

【根因2 — 自愈刷新重试（代码缺口）】
- microsoft-mail.provider.ts：新增 isGraphAuthError + throwMicrosoftGraphError，list/get/send 三处 Graph 失败时区分鉴权失败（401，含 InvalidAuthenticationToken/TokenExpired/CompactToken_ParsingFailure）与其它（502）。
- mail.service.ts：list/get/send 三处 provider 调用包了 withTokenRetry——捕获 401→getAuthorizedAccount(id, forceRefresh=true) 强制刷新+持久化→用新 token 重试一次；仍失败降级 502。只重试一次，不会刷新风暴。
- accounts.service.ts：getAuthorizedAccount 增加 forceRefresh 参数（per-account clientId 刷新逻辑复用既有 buildRefreshOptions）。

【验证】tsc --noEmit exit 0；测试 10/10 通过：
- bind-oauth e2e 5/5（含成功绑定到隔离临时库）
- public client 单测 3/3（含错误体最小化）
- 新增 self-heal：『listMessages: Microsoft 401 后强制刷新并只重试一次』通过
- 新增 401 识别：『InvalidAuthenticationToken 需向上抛 401』通过

【协作说明】本次后端engineer 与我并行动了同一批文件，已合并为一致结果、无残留冲突、构建与测试全绿。
【待办】Task#17 真实账号端到端仍等 owner 账号 AADSTS70000 风控冷却，届时用真实 token 验证 bind→收→发的自愈链路。
@质量与风险总 请复核。完成或有异议本频道回。
