{
  "name": "mail-account-manager",
  "version": "1.0.0",
  "description": "Multi-provider mailbox management application.",
  "private": true,
  "scripts": {
    "start": "powershell -ExecutionPolicy Bypass -File ./start.ps1 -Mode dev",
    "stop": "powershell -ExecutionPolicy Bypass -File ./stop.ps1",
    "build": "npm --prefix mail-backend run build && npm --prefix frontend run build",
    "lint": "npm --prefix mail-backend run typecheck && npm --prefix frontend run lint",
    "test": "npm --prefix mail-backend test && npm --prefix frontend test",
    "validate": "npm run lint && npm test && npm run build",
    "validate:runtime": "powershell -NoProfile -ExecutionPolicy Bypass -File ./deploy/scripts/validate-runtime-gate.ps1"
  },
  "type": "commonjs"
}
